<div class="item-news">
    <div class="item-news__img"  >
       <a href="{{ path('specnews',{'newsid':news.id}) }}"> <img src="{{ asset('img/news/'~news.image) }}" width="100%"></a>
    </div>
    <h2 class="item-news__title">{{news.header}}</h2> {# вот так примерно работают массивы и даты, надо заменить конечно все это на цикл #}
    <p class="item-news__text">{{news.text}}</p>
    <p class="item-news__date">{{news.date|date("d.m.Y")}}</p>
</div>